# ==============================================================================
# Leemage 환경 설정
# 이 파일을 .env로 복사한 후 값을 채워주세요: cp .env.example .env
# ==============================================================================

# ------------------------------------------------------------------------------
# 루트 사용자 인증
# 초기 관리자 계정 설정입니다.
# ------------------------------------------------------------------------------

# 관리자 이메일
ROOT_USER_EMAIL=admin@example.com

# 비밀번호 해시 (bcrypt → base64url 인코딩)
# 생성 방법: npm run root:hash
# 프롬프트에 비밀번호를 입력하면 해시값이 출력됩니다.
ROOT_USER_PASSWORD_HASH_B64=

# ------------------------------------------------------------------------------
# 데이터베이스 (PostgreSQL)
# Docker Compose로 로컬 DB를 실행하거나 외부 DB를 사용할 수 있습니다.
# 로컬 실행: docker compose up -d
# ------------------------------------------------------------------------------

# Docker Compose용 설정
POSTGRES_USER=leemage_user
POSTGRES_PASSWORD=your-db-password
POSTGRES_DB=leemage_db
POSTGRES_PORT=5432

# Prisma 연결 URL
# 형식: postgresql://[user]:[password]@[host]:[port]/[database]
DATABASE_URL=postgresql://leemage_user:your-db-password@localhost:5432/leemage_db

# ------------------------------------------------------------------------------
# 세션 관리 (iron-session)
# 사용자 로그인 세션 암호화에 사용됩니다.
# ------------------------------------------------------------------------------

# 세션 쿠키 이름
IRON_SESSION_COOKIE_NAME=leemage-session

# 세션 암호화 비밀번호 (32자 이상 필수!)
# 생성 방법: openssl rand -base64 32
IRON_SESSION_PASSWORD=your-32-character-or-longer-session-password

# ------------------------------------------------------------------------------
# OCI Object Storage (기본 스토리지)
# Oracle Cloud Infrastructure Object Storage 설정입니다.
# 
# 설정 방법:
# 1. OCI 콘솔 접속: https://cloud.oracle.com
# 2. 사용자 설정 → API Keys → Add API Key
# 3. Private Key 다운로드 후 프로젝트에 저장
# 4. 버킷 생성: Object Storage → Create Bucket
# 5. 버킷 설정에서 PAR 권한 활성화
# ------------------------------------------------------------------------------

# OCI Tenancy OCID (콘솔 → Tenancy 페이지에서 확인)
OCI_TENANCY_OCID=ocid1.tenancy.oc1..your-tenancy-id

# OCI User OCID (사용자 설정 → User Information)
OCI_USER_OCID=ocid1.user.oc1..your-user-id

# API Key Fingerprint (API Key 추가 시 표시됨)
OCI_FINGERPRINT=aa:bb:cc:dd:ee:ff:00:11:22:33:44:55:66:77:88:99

# Private Key 경로 (프로젝트 루트 기준)
OCI_PRIVATE_KEY_PATH=./oci-private-key.pem

# 리전 (버킷 생성 리전)
# 예: ap-seoul-1, us-phoenix-1, eu-frankfurt-1
OCI_REGION=ap-seoul-1

# Object Storage Namespace (콘솔 → Object Storage 페이지에서 확인)
OCI_NAMESPACE=your-namespace

# 버킷 이름
OCI_BUCKET_NAME=your-bucket-name

# Object Storage 호스트 URL
OCI_OBJECT_STORAGE_HOST=https://objectstorage.ap-seoul-1.oraclecloud.com

# ------------------------------------------------------------------------------
# Cloudflare R2 (선택 사항)
# OCI 대신 또는 추가로 Cloudflare R2를 사용할 수 있습니다.
# 프로젝트 생성 시 스토리지 프로바이더를 선택합니다.
#
# 설정 방법:
# 1. Cloudflare 대시보드 → R2 → Create bucket
# 2. R2 → Manage R2 API Tokens → Create API token
# 3. 토큰 권한: Object Read & Write
# 4. Public URL 설정: 버킷 Settings → Public access → Custom domain
# ------------------------------------------------------------------------------

# Cloudflare Account ID (대시보드 URL에서 확인: dash.cloudflare.com/[account-id])
R2_ACCOUNT_ID=your-cloudflare-account-id

# R2 API 토큰
R2_ACCESS_KEY_ID=your-r2-access-key-id
R2_SECRET_ACCESS_KEY=your-r2-secret-access-key

# R2 버킷 이름
R2_BUCKET_NAME=your-r2-bucket-name

# R2 Public URL (커스텀 도메인 또는 r2.dev 도메인)
R2_PUBLIC_URL=https://your-bucket.your-domain.com
